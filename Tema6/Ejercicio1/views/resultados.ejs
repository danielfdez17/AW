<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resultado Encuesta Colores</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container border border-2 border-solid border-black mt-3">
        <h1 class="text-center">Resultados</h1>
        <table class="table w-auto ms-3 border border-2 border-black border-solid">
            <tr>
                <th class="border border-2 border-black border-solid">Respuesta</th>
                <th class="border border-2 border-black border-solid">Número de votos</th>
            </tr>
            <% resultados.forEach(res => {%>
                <tr>
                    <td class="border border-2 border-black border-solid"><%= res.color%></td>
                    <td class="border border-2 border-black border-solid"><%= res.votos%></td>
                </tr>
            <%});%>
        </table>
        <div class="d-flex justify-content-center mb-3">
            <button class="btn btn-primary" type="button"><a class="nav-link" href="/" alt="Volver a la encuesta">Volver a la encuesta</a></button>
        </div>
    </div>
</body>
</html>