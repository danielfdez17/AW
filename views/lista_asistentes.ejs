<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de asistentes</title>
    <%- include('./fragments/head.ejs') %>
</head>
<body>

    <div class="row text-center">
        <div class="col col-12">
            <p class="">Para volver a la página principal haz click en el siguiente <a href="/organizadores">enlace</a></p>
        </div>
    </div>
    
    <div class="d-flex justify-content-center">

        <% if (lista.length > 0) {%>
            <ul class="list-group">
                <% lista.forEach(u => {%>
                    <li class="list-group-item">
                        <%- include('./fragments/usuario.ejs') %>
                    </li>
                <% });%>
            </ul>
        <% } else { %>
            <div class="row text-center">
                <div class="col col-12">
                    <h4 class="mt-3">No hay nadie inscrito en este evento todavía</h4>
                </div>
        <%}%>
    </div>

    
</body>
</html>